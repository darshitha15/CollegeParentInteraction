<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="darshita.com.parentteacherinteraction">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Login" />
        <activity android:name=".parent.ParentActivity" />
        <activity android:name=".faculty.TeacherActivity" />
        <activity android:name=".profilesetup.ProfileSetup" />
        <activity android:name=".studentdetails.StudentDetails" />
        <activity android:name=".details.EditDetails" />
        <activity android:name=".details.DisplayStudDetails" />
        <activity android:name=".details.DisplaySubDetails" />
        <activity android:name=".events.DisplayEvents" />
        <activity android:name=".events.NewEvent" />
        <activity android:name=".query.QueryActivity"></activity>
    </application>

</manifest>